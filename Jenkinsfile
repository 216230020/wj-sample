pipeline {
    agent any

    environment {
        PATH+EXTRA = "/usr/bin"
    }

    stages {
        stage('Webhook Test') {
            steps {
                echo 'Webhook triggered successfully!'
            }
        }
    }
}